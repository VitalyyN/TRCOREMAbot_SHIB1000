services:
  trading-bot: # <-- Измените это имя на уникальное для каждой монеты (например, trading-bot-shib1000)
    build: .
    container_name: trading-bot # <-- Измените это имя на уникальное для каждой монеты (например, trading-bot-shib1000)
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env           # Монтируем .env с хоста
      - ./settings.py:/app/settings.py  # Монтируем settings.py с хоста
      - ./logs:/app/logs           # Директория для логов
      - ./bot_state.json:/app/bot_state.json # Файл состояния робота
    environment:
      - TZ=Europe/Moscow           # Опционально: временная зона
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
